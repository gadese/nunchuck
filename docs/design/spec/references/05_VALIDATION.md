# Validation Guidelines

- [ ] **Naming**: follows `<NN>_<NAME>.md` pattern
- [ ] **Frontmatter**: valid `YAML`, keys are from the allowed set
- [ ] **Body**: Small and focused; less than 100 lines.
- [ ] `uses[*]`: valid paths
- [ ] `executes[*]`: valid paths
- [ ] `produces[*]`: valid paths
