# Determinism Principles

* Prefer explicit inputs and outputs
* Avoid implicit discovery
* Use stable ordering
* Make all state visible and reversible via `clean`
* Ensure repeated invocations behave predictably

> Agentic execution does not relax engineering discipline.
