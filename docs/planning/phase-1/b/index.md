---
status: complete
---

# Phase 1b — Implement `nunchuck` CLI (MVP)

## Brief

Implement a minimal CLI that can list, validate, install, and uninstall skills into a local project using deterministic filesystem operations.

## Tasks

* i — CLI scaffolding — Choose language/runtime, set up package layout, and define command UX
* ii — Core commands — Implement list/validate/install/uninstall with deterministic behavior
* iii — Smoke tests — Add runnable smoke checks and minimal documentation for the CLI
