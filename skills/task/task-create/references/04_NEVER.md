---
description: Forbidden behaviors for this skill.
index: []
---

# Never

- Never manually create `.tasks/<id>.md` when the CLI can do it.
- Never use spaces or special characters in task IDs.
- Never omit `--kind` or `--risk` if the user expects strict categorization.
