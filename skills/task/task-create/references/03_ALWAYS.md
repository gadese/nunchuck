---
description: Non-negotiable invariants for this skill.
index: []
---

# Always

- Always create tasks via the deterministic CLI (`../.shared/scripts/skill.sh create`).
- Always use kebab-case task IDs.
- Always require `--kind` and `--risk` (per legacy instructions).
- Always avoid manual edits to create initial structure (manual edits are allowed after creation).
