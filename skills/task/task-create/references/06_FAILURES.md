# Failures

## Task already exists

- Choose a different ID, or confirm whether to reuse the existing task.

## CLI not runnable

- Instruct the user to install `uv`.
- Re-run `../.shared/scripts/skill.sh validate`.
