# task-create

Create a new task file in `.tasks/<id>.md` from template.

Sets timestamps, computes initial intent hash, and optionally auto-selects the task.

## How to use

- Follow `SKILL.md`.
- Refer to the documents listed in `metadata.references`.
