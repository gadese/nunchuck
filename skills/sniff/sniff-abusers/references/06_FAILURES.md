---
description: Failure modes and handling for sniff-abusers.
index:
  - Cases
---

# Failures

## Cases

- If `git` is unavailable, `validate` fails fast.
- If `uv` is unavailable, `validate` fails fast.
- If the repository is not a git repo, `validate` fails fast.
