# Failures

## No plans found

- Report that `.plan/` is missing or empty.
- Suggest `plan-create` to initialize.

## CLI not runnable

- Instruct the user to install `uv`.
- Re-run `../.shared/scripts/skill.sh validate`.
