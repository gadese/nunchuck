# plan-status

Display plan progress by parsing frontmatter metadata.

Shows status at-a-glance via CLI.

## How to use

- Follow `SKILL.md`.
- Refer to the documents listed in `metadata.references`.
