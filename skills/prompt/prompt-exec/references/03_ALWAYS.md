---
description: Non-negotiable invariants for this skill.
index:
  - Invariants
  - Shared Context
---

# Always

## Invariants

1. Use CLI `status` to check disk state first
2. Verify `status: ready` before proceeding
3. Quote the exact prompt before executing (quote-before-execute)
4. Use CLI `exec` to write receipt and delete artifact

## Shared Context

Refer to `../../.shared/references/03_ALWAYS.md` for skillset-level invariants.
