# prompt-exec

Execute the forged prompt exactly as written.

Requires explicit consent and a ready artifact. Deletes artifact after successful execution.

## How to use

- Follow `SKILL.md`.
- Refer to the documents listed in `metadata.references`.
- Use CLI at `../.shared/scripts/skill.sh` for deterministic operations.

## Artifact

- `.prompt/receipts/`
