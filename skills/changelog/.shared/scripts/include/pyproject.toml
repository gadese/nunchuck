[project]
name = "changelog-cli"
version = "0.1.0"
description = "CLI for changelog skillset operations"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
