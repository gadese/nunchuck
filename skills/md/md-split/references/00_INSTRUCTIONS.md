# Instructions

- Always run scripts for deterministic operations.
- Never "hand split" files unless scripts fail.
- Never guess filenames; derive them from script rules/output.
- Verify split results against the source document.
